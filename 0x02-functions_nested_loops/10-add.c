#include "main.h"
/**
 * add -  a function that adds two integers and returns the result
 * @n1: the parameter that used for the addition operation
 * @n2: the parameter that used for the addition operation
 * Return: sum of n1 and n2
 */
int add(int n1, int n2)
{
	int result;

	result = n1 + n2;
	return (result);
}
