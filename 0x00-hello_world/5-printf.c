#include <stdio.h>
/**
 * main - a prgram that prints a line using printf function
 * Return: 0 if the program compiled with no errors
 */
int main(void)
{
printf("\"with proper grammar, but the outcome is a piece of art");
return (0);
}
